/* Section 2 · Tagline — the payoff. Fixed copy, carries alone. Deliberately NOT a
   heading (not h1/h2): a display statement in Sora. A bright porcelain band so it
   reads as a clean break from the dark hero rather than blending into it. */
export default function Tagline() {
  return (
    <section className="tagline-band tagline-light" aria-label="Tagline">
      <div className="wrap">
        <p className="tagline">Organic Growth. Compounded.</p>
      </div>
    </section>
  );
}
